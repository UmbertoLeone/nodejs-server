{
  "name": "Togetherfy",
  "version": "1.0.0",
  "description": "Manage listening queue on Spotify",
  "main": "index.js",
  "scripts": {
    "build": "rimraf dist && swc ./src -d dist",
    "db:console": "docker compose run postgres psql -h postgres -U postgres -d togetherfy",
    "db:migrate": "knex migrate:latest",
    "docker:db:migrate": "docker compose run backend pnpm db:migrate",
    "docker:db:migrate:undo": "docker compose run backend pnpx knex migrate:down",
    "docker:seed:dev": "docker compose run backend pnpm ts-node scripts/seed/dev.ts",
    "rebuild:be": "docker compose build backend && docker compose rm --force --stop backend && docker compose up -d backend",
    "start": "node dist/index.js",
    "start:docker": "pnpm build && pnpm start"
  },
  "author": "Umberto Leone",
  "license": "MIT",
  "dependencies": {
    "@prisma/client": "^5.2.0",
    "@types/node": "^20.5.7",
    "express": "^4.18.2",
    "knex": "^2.5.1",
    "morgan": "^1.10.0",
    "pg": "^8.11.3",
    "typescript": "^5.2.2"
  },
  "devDependencies": {
    "@swc/cli": "^0.1.62",
    "@swc/core": "^1.3.80",
    "@types/express": "^4.17.17",
    "@types/morgan": "^1.9.5",
    "@typescript-eslint/eslint-plugin": "^6.4.1",
    "@typescript-eslint/parser": "^6.4.1",
    "eslint": "^8.48.0",
    "rimraf": "^5.0.1",
    "ts-node": "^10.9.1"
  },
  "packageManager": "pnpm@8.7.0"
}
